group = 'kz.bsbnb.usci.sync'
version '0.1.0'

dependencies {
    compile project(':eav:eav-api')
    
    compile('org.springframework.cloud:spring-cloud-starter-openfeign')
}

jar {
    baseName = 'usci-sync-api'
    version = '0.1.0'
    enabled = true
}

bootJar {
    enabled = false
}