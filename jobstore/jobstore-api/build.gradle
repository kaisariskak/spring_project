group = 'kz.bsbnb.usci.jobstore'
version = '0.1.0'

dependencies {
    compile project(':model')
    compile project(':util:util-api')
}

jar {
    baseName = 'usci-jobstore-api'
    version = '0.1.0'
    enabled = true
}

bootJar {
    enabled = false
}