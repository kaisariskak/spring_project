group = 'kz.bsbnb.usci.util'
version '0.1.0'

dependencies {
    compile project(":model")

    compile('org.springframework.cloud:spring-cloud-starter-openfeign')
}

jar {
    baseName = 'usci-util-api'
    version = '0.1.0'
    enabled = true
}

bootJar {
    enabled = false
}